const ganttConfig = {
  columns: [{ type: "name", field: "name", width: 250 }],
  viewPreset: "weekAndDayLetter",
  barMargin: 10,
  selectionMode: {
    multiSelect: false,
  },
};

export { ganttConfig };

